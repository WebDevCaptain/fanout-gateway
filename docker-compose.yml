services:
  valkey:
    image: valkey/valkey:9.0.3-alpine 
    container_name: market-valkey
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: always
